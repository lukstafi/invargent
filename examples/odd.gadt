newtype Bar
newtype List : num

newcons LNil : List 0
newcons LCons : ∀n. Bar * List(n) ⟶ List(n+1)

newtype Bool : num
newcons True : Bool(1)
newcons False : Bool(0)

let rec odd =
  function LNil -> False
    | LCons (x, LNil) -> True
    | LCons (x, LCons (y, z)) -> odd z
