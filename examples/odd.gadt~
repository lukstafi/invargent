newtype Bar
newtype List : nat

newcons LNil : List 0
newcons LCons : for all (n) : Bar * List(n) ⟶ List(n+1)

newtype Bool : nat
newcons True : Bool(1)
newcons False : Bool(0)

let rec odd =
  function LNil -> False
    | LCons (x, LNil) -> True
    | LCons (x, LCons (y, z)) -> odd z
